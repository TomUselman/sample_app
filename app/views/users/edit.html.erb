<h1>Edit user</h1>

<%= form_for(@user) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>

<div>
  <%= link_to gravatar_for @user, :target=>'_blank', :href=>'http://gravatar.com/emails' %>
</div>